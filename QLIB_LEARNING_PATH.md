# Qlib 学习路线图：从零到一

本学习路线旨在为想要掌握 Qlib 的学习者提供一条清晰、循序渐进的路径。它遵循"先理论、后实践，先宏观、后微观"的原则，帮助您平稳地从入门到精通。

---

### 阶段零：基础准备 (Prerequisites)

在深入 Qlib 之前，坚实的基础是必不可少的。如果已具备相关知识，可跳过此阶段。

#### 1. Python 编程基础 (必备)
*   **目标**：熟练使用 Python 进行数据处理。
*   **核心库**：
    *   **`Pandas`**: 精通 DataFrame 操作，包括数据读取、清洗、筛选、分组、合并等。
    *   **`NumPy`**: 掌握数组操作，理解科学计算基础。
    *   **`Matplotlib` / `Seaborn`**: 掌握至少一种数据可视化库，用于分析策略结果。

#### 2. 量化金融入门 (理论)
*   **目标**：理解量化交易的基本术语和逻辑。
*   **核心概念**：
    *   **Alpha (α) / 因子**：理解超额收益和因子的概念。
    *   **回测 (Backtesting)**：了解其目的和基本流程。
    *   **关键绩效指标 (KPIs)**：必须理解**年化收益率 (Annualized Return)**、**夏普比率 (Sharpe Ratio)** 和 **最大回撤 (Max Drawdown)**。
    *   **交易成本**：了解手续费、印花税和滑点对策略的影响。

#### 3. 机器学习入门 (理论)
*   **目标**：理解监督学习的基本流程。
*   **核心概念**：
    *   **监督学习**：区分特征 (Features) 和标签 (Labels)。
    *   **模型训练**：理解训练集、验证集、测试集的划分。
    *   **模型评估**：了解 IC (信息系数) 和 Rank IC 的概念。

---

### 阶段一：首次接触 (Getting Started with Qlib)

本阶段的目标是成功运行 Qlib，并对它的工作流程有一个整体的感性认识。

1.  **安装 Qlib**
    *   **动作**：遵循官方文档，使用 `pip install pyqlib` 安装。
    *   **重点**：确保环境配置正确，安装成功。

2.  **下载官方数据**
    *   **动作**：找到并运行 Qlib 提供的 `get_data.py` 脚本，下载官方提供的 A 股或美股示例数据。
    *   **目标**：让 Qlib "有米下锅"。

3.  **运行第一个官方示例**
    *   **动作**：在 Qlib 的 GitHub 仓库中找到 `examples` 文件夹。找到 `workflow_by_code.py` 或 `workflow_by_config.py` 并尝试运行它们。
    *   **目标**：亲眼看到一个完整的 AI 量化策略（从数据处理、模型训练到回测）是如何自动跑完的。关键是建立宏观认识，不必深究代码细节。
    *   **观察**：查看运行后生成的 `backtest_result.pkl` 和相关图表，将结果与阶段零的理论知识对应起来。

---

### 阶段二：核心模块深潜 (Diving into Core Components)

本阶段的目标是理解 Qlib 的主要模块，知道如何使用和配置它们。

1.  **数据模块 (Data Layer)**
    *   **学习**：阅读 Qlib 关于 `Qlib-Data` 的文档，理解其数据存储和加载机制。
    *   **实践**：
        *   尝试加载不同的时间段、不同的股票代码。
        *   学习 `D.features()` 函数，加载不同特征，例如 `$open`, `$close`, `$volume` 以及 `Ref($close, 1)`。

2.  **模型与预测 (Model Training)**
    *   **学习**：浏览 Qlib 的 `Model Zoo` (模型库)，了解其内置模型（如 LightGBM, GATs）。
    *   **实践**：
        *   修改官方示例，更换不同的模型。
        *   观察不同模型对最终回测结果的影响。

3.  **回测引擎 (Backtesting Engine)**
    *   **学习**：阅读回测模块的文档，理解回测的配置项。
    *   **实践**：
        *   修改回测配置，调整**交易成本**（手续费、滑点），观察其对策略收益的影响。
        *   修改**持仓股票数量**，观察策略表现的变化。

---

### 阶段三：定制与扩展 (Customization & Extension)

本阶段的目标是让 Qlib 成为您自己的研究工具。

1.  **自定义因子 (最重要的定制化)**
    *   **学习**：掌握 Qlib 的表达式 (Expression) 引擎和运算符。
    *   **实践**：
        *   **简单因子**：编写自己的因子，例如 `(Ref($close, 1) - Ref($close, 6)) / Ref($close, 6)` (5日动量因子)。
        *   **复杂因子**：学习使用 `rolling` 运算符，编写更复杂的因子，如 `Mean($close, 20)` (20日均线)。
        *   **分析因子**：使用 Qlib 的因子分析工具，评估您自定义因子的有效性。

2.  **接入自己的数据**
    *   **学习**：阅读关于如何将自己的数据（如 CSV 文件）转换为 Qlib 可用格式的文档 (`dump_bin.py`)。
    *   **实践**：尝试导入一小部分您自己的数据，并用它来跑通一个完整的流程。

3.  **(可选) 集成自己的模型**
    *   对于高级用户，学习如何将自己的 PyTorch 或 Scikit-learn 模型包装成 Qlib 可以调用的格式。

---

### 阶段四：迈向实战 (Towards Real World)

1.  **参数调优**：学习使用 `hyperopt` 等工具对模型和策略参数进行调优。
2.  **鲁棒性分析**：进行滚动回测 (Rolling Backtest)，或在不同市场阶段（牛市、熊市、震荡市）进行回测，检验策略的稳定性。
3.  **模拟交易与实盘对接**：学习 Qlib 的预测模块 (`online` 模式)，了解如何每天生成预测信号，并思考如何将其转化为交易指令。